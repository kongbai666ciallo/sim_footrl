"""RL agents configuration package for footrl my_task.

Provides entry points for different RL libraries (rsl-rl, rl-games, sb3, skrl).
Currently only rsl-rl python config is implemented; yaml configs are placeholders.
"""
